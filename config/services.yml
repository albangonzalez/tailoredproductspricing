services:
    _defaults:
        autowire: true
        autoconfigure: false
        public: true

    # Adapters (read-only access to core-owned tables)
    PrestaShop\Module\TailoredProducts\Adapter\ColorAttributeProvider:
        arguments:
            $connection: '@doctrine.dbal.default_connection'

    # Form modifiers
    PrestaShop\Module\TailoredProducts\Form\Modifier\ProductFormModifier:
        arguments:
            $formBuilderModifier: '@form.form_builder_modifier'
            $contextLangId: "@=service('prestashop.adapter.legacy.context').getContext().language.id"
            $contextShopId: "@=service('prestashop.adapter.legacy.context').getContext().shop.id"

    PrestaShop\Module\TailoredProducts\Form\Modifier\CombinationFormModifier: ~
        # autowires CombinationEnablementChecker by type

    # Services
    PrestaShop\Module\TailoredProducts\Service\ProductConfigManager:
        arguments:
            $contextShopId: "@=service('prestashop.adapter.legacy.context').getContext().shop.id"

    PrestaShop\Module\TailoredProducts\Service\CombinationEnablementChecker:
        arguments:
            $connection: '@doctrine.dbal.default_connection'

    PrestaShop\Module\TailoredProducts\Service\CustomizationFieldRegistry:
        arguments:
            $connection: '@doctrine.dbal.default_connection'
            $translator: '@translator'

    # Repositories
    PrestaShop\Module\TailoredProducts\Repository\ProductConfigRepository:
        class: PrestaShop\Module\TailoredProducts\Repository\ProductConfigRepository
        factory: ['@doctrine.orm.default_entity_manager', 'getRepository']
        arguments:
            - PrestaShop\Module\TailoredProducts\Entity\TpProductConfig

    PrestaShop\Module\TailoredProducts\Repository\CombinationConfigRepository:
        class: PrestaShop\Module\TailoredProducts\Repository\CombinationConfigRepository
        factory: ['@doctrine.orm.default_entity_manager', 'getRepository']
        arguments:
            - PrestaShop\Module\TailoredProducts\Entity\TpCombinationConfig
